/// <summary>
/// Stores session-wide user data that persists while the application is running.
/// </summary>
public static class UserSession
{
    // The currently logged-in username, defaulting to Guest.
    public static string CurrentUsername = "Guest";
}